"""Package initialization module for the Foodshare backend.

This module initializes the backend package and sets up the necessary imports
and configurations for the application to function properly. It serves as the
entry point for the src package and ensures all required components are available
for other modules to import and use.

The package provides:
- Core application components (Quart app, database manager, storage service)
- Authentication routes and middleware
- Database helpers and data models
- Storage management capabilities

Key features:
- Package-level imports for easy access to core components
- Application initialization setup
- Consistent module structure for the backend
"""
